import { makeStyles } from "@material-ui/core/styles";

const style = makeStyles((theme) => ({
  buttonsC: {
    marginTop: "20px",
    textAlign: "right",
    padding: "4px",
  },
}));

export default style;
