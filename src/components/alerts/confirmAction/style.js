import { makeStyles } from "@material-ui/core/styles";

const style = makeStyles((theme) => ({
  messageC: {
    textAlign: "center",
  },
  buttonsC: {
    marginTop: "20px",
    textAlign: "center",
    padding: "4px",
  },
}));

export default style;
